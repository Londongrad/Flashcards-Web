@model ImageViewModel

@{
    Layout = null;
}

<form id="formEmail" asp-area="Account" asp-controller="Account" asp-action="UpdateAvatar" method="post">

    <div class="border p-3 mt-4">

        <div class="input-group row mb-3">
            <label asp-for="@Model.ImageURL" class="input-group-text bg-dark text-light"></label>
            <input asp-for="@Model.ImageURL" class="form-control bg-dark text-light">
        </div>

        <div class="row">
            <div class="col-5 m-auto">
                <button type="submit" class="btn btn-primary">
                    Save changes
                </button>
            </div>
        </div>
    </div>
</form>

<script>
    $(function() {
        $("#formEmail").submit(function(e) {
            e.preventDefault();
            var form = $(this);
            popupPost(form);
        });
    });
</script>