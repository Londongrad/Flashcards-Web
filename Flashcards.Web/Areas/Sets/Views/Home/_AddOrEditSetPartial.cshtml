@* @model SetViewModel

@{
    Layout = null;
}

    <div class="border p-3 mt-4">

        <div class="input-group mb-3 row">
            <label asp-for="Name" class="input-group-text text-light bg-dark"></label>
            <input asp-for="Name" class="form-control text-light bg-dark">
            <span asp-validation-for="Name" class="text-danger mt-2"></span>
        </div>

        <div class="row">
            <div class="col-5 m-auto">
                <button type="submit" class="m-auto btn btn-primary form-control border-black border-1 text-white">
                    @(Model.Id == 0 ? "Create set" : "Update set")
                </button>
            </div>
        </div>
    </div> *@
